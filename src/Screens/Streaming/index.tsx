import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const StreamingScreen = () => {
  return (
    <View>
      <Text>StreamingScreen</Text>
    </View>
  );
};

export default StreamingScreen;

const styles = StyleSheet.create({});
